//a mix of filter and map that filters only numbers and multiply them by them and return the result.


//a mix of filter and map that filters only numbers and multiply them by them and return the result.


let mix = "A13BS2vdej92384dskov32ZX";
/*1) let newMix = mix.split("").filter(function(nM){
    return parseInt(nM);
}).map(function(nN){
    return nN * nN;
}).join("");

    console.log(newMix);
*/

/*2) let newMix = mix.split("").filter(function(nM){
    return !isNaN(parseInt(nM));
}).map(function(nN){
    return nN * nN;
}).join("");

console.log(newMix);
*/

